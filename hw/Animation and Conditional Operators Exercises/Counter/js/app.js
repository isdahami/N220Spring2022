// Isaac Hamilton
// 1/18/2022
//n220


// starts the "counter" at 0
let counter = 0;

// creates the function draw
function draw() {
    // draws the function to add 1 over and over again
    counter = counter + 1;
    // prints JS into the console, in this instance you will see numbers going up
    console.log(counter);
}